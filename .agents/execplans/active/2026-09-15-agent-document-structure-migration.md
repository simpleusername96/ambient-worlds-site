---
type: plan
status: active
created_at: 2026-09-15T13:42:10+09:00
scope: repository agent-document structure
related:
  - AGENTS.md
  - .agents/AGENTS.md
  - docs/AGENTS.md
  - docs/PROJECT.md
  - README.md
  - llms.txt
---

# Establish Ambient Worlds Site Agent-Document Structure

Create the current `agent-governor` owners for this existing static publication without changing its reviewed runtime or public content.

## Purpose and Boundaries

- Add a compact project contract and authority map based on the inspected static shell, world registry, adapters, playback/audio owners, publication metadata, and license files.
- Add concise root, `.agents`, and `docs` scoped instructions plus active/archive plan lifecycle.
- Do not change HTML, CSS, JavaScript, embedded world source, screenshots, SEO/public copy, headers, licenses, deployment settings, or live-site behavior.
- No legacy lifecycle-classification dependency or prior agent-document tree exists.

## Discovery Closure

| Concern | Verified state | Locked decision | Tasks |
| --- | --- | --- | --- |
| Project contract | No agent guidance or compact contract exists | Create `docs/PROJECT.md` from README, `llms.txt`, shell markup, and runtime owner inspection | 1.1 |
| Scoped owners | No `.agents` or docs-scoped instructions exist | Add minimal current owner and 24-hour plan lifecycle rules | 1.2 |
| Runtime | Dependency-free static site with Journey/Stillwater adapters, optional sound, pause/random/focus controls, and Cloudflare Pages publication | Preserve all runtime files unchanged and route their responsibilities only | 2.1 |
| Validation | No test/package runner exists | Use diff, required-path, reference, dependency, scope, and Git delivery checks | 3.2 |
| Git | Clean `main` tracks `origin/main` | Commit/push this plan, structure, and archived completion checkpoints | 3.3 |

## Tasks

- [x] **1.1** Create `docs/PROJECT.md` with purpose, scope, accepted experience, implementation owners, safety/licensing constraints, publication owners, and focused validation.
  - Accept: a new session can route work without reading source files broadly.
  - Evidence: the contract routes the inspected shell, registry, adapter, playback, audio, world, metadata, and license owners.
- [x] **1.2** Create root `AGENTS.md`, `.agents/AGENTS.md`, and `docs/AGENTS.md` with current routing, language, work-state, retention, and authority rules.
  - Accept: project truth, work state, and documentation have one owner each.
  - Evidence: the three scoped entry points directly route current owners and define active/archive lifecycle without a compatibility policy.
- [x] **2.1** Verify that all named runtime/publication/license owners exist and no product file is modified.
  - Accept: the migration is governance-only.
  - Evidence: all 21 required paths exist and the working diff contains no public or runtime path.
- [x] **3.1** Apply `$occam-ablation` with the Implementation target.
  - Accept: no redundant project map, duplicate policy, or compatibility pointer is added.
  - Evidence: no safe subtraction remains; root routing, work state, docs scope, and compact project truth are separate responsibilities.
- [x] **3.2** Run `git diff --check`, path/link/dependency checks, and changed-scope verification.
  - Accept: all checks pass and public/runtime files remain byte-identical.
  - Evidence: diff checks pass; required paths resolve; removed-dependency references are zero; only governance/docs paths changed.
- [ ] **3.3** Commit/push the structure, then archive this completed plan and push the final checkpoint.
  - Accept: local `HEAD` matches `origin/main`, active count is zero, and the worktree is clean.

## Rework and Completion Controls

- Replan if accurate contract creation would require changing public claims, licenses, or runtime behavior.
- Do not add tests or run browser checks because no executable input changes and no runner is defined.
- Resume at the first unchecked task and do not repeat passing checks without changed inputs.
